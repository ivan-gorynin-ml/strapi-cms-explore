'use strict';

/**
 * account-holder-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::account-holder-profile.account-holder-profile');
